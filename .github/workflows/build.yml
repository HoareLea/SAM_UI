name: Build (Windows)

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    # Guard: if this workflow ever gets merged upstream (e.g. HoareLea),
    # it will show as "skipped" there and won't break their CI.
    if: github.repository_owner == 'SAM-BIM'
    runs-on: windows-2022

    env:
      MSBUILDDISABLENODEREUSE: "1"
      ORG_NAME: "SAM-BIM"
      REPO_NAME: "SAM_UI"
      NUGET_XMLDOC_MODE: "skip"

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          path: ${{ env.REPO_NAME }}

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.x"

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: nuget/setup-nuget@v2
        with:
          nuget-version: "6.x"

      - name: Clone dependency repos (siblings)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $org   = '${{ env.ORG_NAME }}'
          $repo  = '${{ env.REPO_NAME }}'
          $token = '${{ secrets.ORG_REPO_TOKEN }}'

          function Clone-Repo([string]$name) {
            if (Test-Path $name) { return }

            $url = if ($token) {
              "https://$token@github.com/$org/$name.git"
            } else {
              "https://github.com/$org/$name.git"
            }

            Write-Host "Cloning $name"
            git clone --depth 1 $url $name
            if ($LASTEXITCODE -ne 0) {
              throw "Failed to clone $name. If it is private, check ORG_REPO_TOKEN access."
            }
          }

          # Keep current repo LAST. Do not duplicate SAM_UI earlier in the list.
          $buildOrder = @(
            'SAM'
            'SAM_Systems'
            'SAM_Windows'
            'SAM_Mollier'
            'SAM_gbXML'
            'SAM_SolarCalculator'
            'SAM_Tas'
            'SAM_Excel'
            'SAM_GEM'
            'SAM_LadybugTools'
            'SAM_Solver'
            $repo
          )

          # Clone everything except the last one (already checked out by Actions)
          $deps = $buildOrder[0..($buildOrder.Count-2)]
          foreach ($r in $deps) {
            Clone-Repo $r
          }

          # Ensure ReferencePath exists even before SAM_Windows builds
          New-Item -ItemType Directory -Force -Path "SAM_Windows\build" | Out-Null

      - name: Restore + Rebuild in order (CI props like SAM_Deploy)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $repo = '${{ env.REPO_NAME }}'
          $windowsRef = (Resolve-Path 'SAM_Windows\build').Path

          # IMPORTANT: keep these as an ARRAY of args (do NOT join into one string)
          $common = @(
            '/m:1'
            '/nr:false'
            '/v:m'
            '/p:Configuration=Release'
            '/p:UseSharedCompilation=false'
            '/p:RunPostBuildEvent=OnOutputUpdated'
            "/p:ReferencePath=$windowsRef"
          )

          # Keep current repo LAST. SAM_Solver must be built before SAM_UI.
          $buildOrder = @(
            'SAM'
            'SAM_Systems'
            'SAM_Windows'
            'SAM_Mollier'
            'SAM_gbXML'
            'SAM_SolarCalculator'
            'SAM_Tas'
            'SAM_Excel'
            'SAM_GEM'
            'SAM_LadybugTools'
            'SAM_Solver'
            $repo
          )

          # Convert build order -> solution paths (robust: first .sln in each folder)
          $solutions = foreach ($r in $buildOrder) {
            $expected = Join-Path $r "$r.sln"
            if (Test-Path $expected) {
              (Resolve-Path $expected).Path
              continue
            }

            $sln = Get-ChildItem -Path $r -Filter *.sln -File -ErrorAction SilentlyContinue | Select-Object -First 1
            if (-not $sln) { throw "No .sln found under: $r" }
            $sln.FullName
          }

          foreach ($sln in $solutions) {
            Write-Host "==> Restore: $sln"
            & msbuild $sln /t:Restore @common
            if ($LASTEXITCODE -ne 0) { throw "MSBuild restore failed: $sln" }
          }

          foreach ($sln in $solutions) {
            Write-Host "==> Rebuild: $sln"
            & msbuild $sln /t:Rebuild @common
            if ($LASTEXITCODE -ne 0) { throw "MSBuild rebuild failed: $sln" }
          }

      - name: Upload build folders (optional)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.REPO_NAME }}-build-folders
          path: |
            **\build\*
          if-no-files-found: warn
